version: 1
status: managed_by_aiquantbase

mode_config_files:
  - modes/research/factor_research.yaml
  - modes/strategy/discrete_stock.yaml

provider_manifest: manifest.yaml
query_templates: query_templates.yaml

note: >
  Capability requirements are managed by AIQuantBase. AlphaBlocks consumes
  these capabilities through the AIQuantBase SDK/API and should not be required
  to expose local mode config paths in the Studio UI.
